"""Presentation-only helpers: charts and reports.

Nothing in this package computes a statistical result. It only renders results
computed in the parent package (loading, analysis, statistics). Kept separate
so the code that affects the numbers stays small and easy to audit.
"""
